# NumPy Mini Lab (Lesson 9)

## Goal

Hands-on practice with arrays, shapes, and basic operations.

## Starter Code

```python
import numpy as np

v = np.array([1, 2, 3])
M = np.array([[1, 2], [3, 4]])

print(v.shape)
print(M.shape)
```

## Tasks

1. Create at least two vectors and two matrices.
2. Print `.shape` for each.
3. Compute one dot product between two vectors.
4. Try one simple matrix operation (e.g. add two matrices of same shape).
5. Save notebook as `numpy-mini-lab.ipynb` in `ai-math-bridge/`.
6. Commit to GitHub with a clear message.

## What You Should Learn

- array
- shape
- vector vs matrix
- simple dot product

## Exit Ticket

What does `.shape` tell you?

Use Google Colab, Jupyter, or local Python. No heavy linear algebra required.
